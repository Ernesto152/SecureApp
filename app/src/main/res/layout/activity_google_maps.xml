<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:fab="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/googleMaps">

    <fragment
        android:id="@+id/map"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context="app.ernestochira.com.secureapp.GoogleMapsActivity"/>

    <com.getbase.floatingactionbutton.FloatingActionsMenu
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/menuFloatingActionMenu"
        android:layout_margin="16dp"
        fab:fab_labelsPosition="left"
        app:layout_anchorGravity="bottom|right"
        android:layout_gravity="end|center_horizontal|bottom"
        app:fab_labelStyle="@style/Tags">
        <com.getbase.floatingactionbutton.FloatingActionButton
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:fab_size="mini"
            fab:fab_icon="@drawable/ic_web_asset_black_24dp"
            fab:fab_title="Noticias"
            app:fab_colorNormal="@color/colorPrimary"
            app:fab_colorPressed="@color/colorDivider"
            android:id="@+id/noticesFloatingActionButton"/>
        <com.getbase.floatingactionbutton.FloatingActionButton
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:fab_size="mini"
            fab:fab_icon="@drawable/ic_assignment_black_24dp"
            fab:fab_title="Registrar incidencia"
            app:fab_colorNormal="@color/colorPrimary"
            app:fab_colorPressed="@color/colorDivider"
            android:id="@+id/incidenceFloatingActionButton"/>

    </com.getbase.floatingactionbutton.FloatingActionsMenu>
</android.support.design.widget.CoordinatorLayout>